{
  "JwtSettings": {
    "Secret":  "MySecretForJWTTokens?CHANGE IT LATER!!"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "OneClickDesktop": { //Configuration for OneClickDesktop system
    "OverseerId": "overseer-test", //Overseer system-wide unique id
    "RabbitMQHostname": "localhost", //Internal RabbitMQ broker access address
    "RabbitMQPort": "5672", //Internal RabbitMQ broker access port
    "SessionWaitingTimeout": "15", //Timeout in minutes for machine shoutdown after lossing connection with client
    "ModelUpdateInterval":  "10" //Model requests to virtualization servers interval (in seconds)
  }
}
